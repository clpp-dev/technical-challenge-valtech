mutation updateCF($acronym: String!, $document: DocumentInput!) {
  updateDocument(acronym: $acronym, document: $document) @context(provider: "vtex.store-graphql@2.x") {
    documentId
    id
    href
  }
}

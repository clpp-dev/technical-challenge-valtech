mutation editDocument($acronym: String!, $documentId: String!, $document: DocumentInput!) {
  updateDocument(acronym: $acronym, documentId: $documentId, document: $document) @context(provider: "vtex.store-graphql@2.x") {
    documentId
    id
    href
  }
}
